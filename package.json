{
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "scripts": {
    "test": "jest --watch",
    "test_onetime": "jest",
    "path_coverage": "jest --coverage"
  }
}
